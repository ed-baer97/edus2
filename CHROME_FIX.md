# 🔧 Решение проблемы с Chrome на Render

## Проблема
```
Message: session not created: Chrome instance exited.
```

## Что было исправлено

### 1. Улучшен build.sh
- ✅ Добавлены все необходимые системные зависимости
- ✅ Улучшена установка Google Chrome
- ✅ Добавлена проверка установки Chrome
- ✅ Создание символических ссылок для Chrome

### 2. Обновлен mektep_scraper.py
- ✅ Добавлена поддержка webdriver-manager для автоматической установки ChromeDriver
- ✅ Улучшены опции Chrome для headless режима
- ✅ Добавлена обработка ошибок с fallback на минимальные опции
- ✅ Убрана опция `--single-process` (может вызывать проблемы)
- ✅ Добавлены дополнительные опции для стабильности

### 3. Ключевые изменения

#### Опции Chrome для Render (headless):
```python
--headless=new          # Новый headless режим
--no-sandbox           # Обязательно для Docker/Render
--disable-dev-shm-usage # Обязательно для ограниченной памяти
--disable-gpu
--window-size=1920,1080
```

#### Автоматическая установка ChromeDriver:
- Используется `webdriver-manager` для автоматической установки
- Если не работает, пробуется стандартный способ
- В крайнем случае - минимальные опции

## Проверка после деплоя

1. **Проверьте логи сборки на Render:**
   - Должно быть: `✓ Google Chrome установлен: ...`
   - Должно быть: `✓ Сборка завершена успешно!`

2. **Проверьте логи при запуске:**
   - Должно быть: `✓ ChromeDriver установлен через webdriver-manager`
   - Должно быть: `✓ Браузер запущен успешно`

3. **Если все еще есть ошибки:**
   - Проверьте, что Chrome установлен: в логах сборки должно быть сообщение об установке Chrome
   - Проверьте переменные окружения: `HEADLESS=true` должен быть установлен
   - Проверьте логи приложения на Render Dashboard

## Дополнительные шаги (если проблема сохраняется)

### Вариант 1: Проверка установки Chrome
В логах сборки должно быть:
```
✓ Google Chrome установлен: Google Chrome 120.x.x
```

Если нет - проверьте логи сборки на ошибки установки.

### Вариант 2: Ручная установка ChromeDriver
Если webdriver-manager не работает, можно добавить в build.sh:
```bash
# Установка ChromeDriver вручную
CHROMEDRIVER_VERSION=$(google-chrome --version | grep -oP '\d+\.\d+\.\d+' | head -1)
wget -q https://chromedriver.storage.googleapis.com/LATEST_RELEASE_${CHROMEDRIVER_VERSION} -O /tmp/chromedriver_version.txt
CHROMEDRIVER_VER=$(cat /tmp/chromedriver_version.txt)
wget -q https://chromedriver.storage.googleapis.com/${CHROMEDRIVER_VER}/chromedriver_linux64.zip
unzip chromedriver_linux64.zip -d /usr/local/bin/
chmod +x /usr/local/bin/chromedriver
```

### Вариант 3: Использование Chromium вместо Chrome
Если Chrome не устанавливается, можно использовать Chromium:
```bash
apt-get install -y chromium chromium-driver
```

## Контрольный список

- [ ] `build.sh` обновлен и выполняется без ошибок
- [ ] Chrome установлен (проверьте логи сборки)
- [ ] Переменная `HEADLESS=true` установлена на Render
- [ ] Переменные `EDUS_LOGIN` и `EDUS_PASSWORD` установлены
- [ ] Логи приложения показывают успешный запуск браузера

## Если проблема не решена

1. Проверьте полные логи на Render Dashboard
2. Убедитесь, что используется последняя версия кода
3. Попробуйте пересобрать сервис на Render
4. Проверьте, что все зависимости установлены корректно
