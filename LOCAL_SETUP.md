# 🖥️ Настройка для локальной разработки

## Проблема: Браузер не открывается

Если браузер не открывается локально, проверьте следующие настройки:

## Решение 1: Проверьте переменные окружения

### Вариант A: Создайте файл `.env` в корне проекта

Создайте файл `.env` со следующим содержимым:

```env
HEADLESS=false
```

**Важно**: Файл `.env` уже в `.gitignore`, поэтому он не будет закоммичен.

### Вариант B: Убедитесь, что переменная HEADLESS не установлена в системе

Проверьте переменные окружения Windows:
1. Откройте "Переменные среды" в Windows
2. Убедитесь, что переменная `HEADLESS` не установлена или установлена в `false`

### Вариант C: Установите переменную перед запуском

В PowerShell:
```powershell
$env:HEADLESS="false"
python app.py
```

В Command Prompt:
```cmd
set HEADLESS=false
python app.py
```

## Решение 2: Проверьте логи

При запуске скрапера вы должны увидеть в консоли:

```
Режим запуска: ОБЫЧНЫЙ (браузер откроется)
✓ Браузер откроется в обычном режиме
✓ Браузер запущен успешно
```

Если видите:
```
Режим запуска: HEADLESS
⚠ Внимание: Браузер работает в headless режиме и не будет виден
```

Значит переменная `HEADLESS=true` установлена где-то в системе или в `.env` файле.

## Решение 3: Создайте .env файл вручную

1. Создайте файл `.env` в корне проекта (там же, где `app.py`)
2. Добавьте строку:
   ```
   HEADLESS=false
   ```
3. Перезапустите приложение

## Проверка

После настройки:

1. Запустите приложение: `python app.py`
2. Откройте браузер и перейдите на `http://localhost:5000`
3. Нажмите "Запустить" скрапер
4. Должно открыться окно Chrome браузера

## Автоматическая vs Ручная авторизация

### Если указаны логин и пароль в .env:
```env
HEADLESS=false
EDUS_LOGIN=your_login@example.com
EDUS_PASSWORD=your_password
```
→ Будет автоматическая авторизация (браузер откроется, но авторизуется сам)

### Если логин и пароль не указаны:
```env
HEADLESS=false
```
→ Браузер откроется, и вы сможете авторизоваться вручную

## Для Render (production)

На Render используйте:
- `HEADLESS=true` (установлено в render.yaml)
- `EDUS_LOGIN` и `EDUS_PASSWORD` (обязательно для автоматической авторизации)
