# 🚀 Быстрый старт: Деплой на Render

## Что было сделано

✅ Обновлен `requirements.txt` - добавлены gunicorn и python-dotenv  
✅ Обновлен `app.py` - поддержка переменной PORT от Render  
✅ Обновлен `mektep_scraper.py` - поддержка headless режима  
✅ Обновлен `config.py` - использование переменных окружения  
✅ Создан `render.yaml` - конфигурация для Render  
✅ Создан `build.sh` - установка Chrome/Chromium  
✅ Создан `.gitignore` - исключение ненужных файлов  

## 📋 Шаги для деплоя

### 1. Подготовка репозитория

```bash
# Убедитесь, что все файлы закоммичены
git add .
git commit -m "Подготовка к деплою на Render"
git push
```

### 2. Создание сервиса на Render

1. Откройте [Render Dashboard](https://dashboard.render.com)
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите ваш репозиторий (GitHub/GitLab/Bitbucket)
4. Render автоматически обнаружит `render.yaml` и применит настройки

### 3. Настройка переменных окружения

В разделе **Environment** добавьте:

- `EDUS_LOGIN` = ваш логин для mektep.edu.kz
- `EDUS_PASSWORD` = ваш пароль

⚠️ **Важно**: Не коммитьте реальные учетные данные в код!

### 4. Деплой

Нажмите **"Create Web Service"** и дождитесь завершения сборки.

## ⚙️ Настройки

- **Build Command**: `chmod +x build.sh && ./build.sh`
- **Start Command**: `gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --threads 2 --timeout 300`
- **Python Version**: 3.11.0
- **Headless Mode**: включен автоматически

## 🔍 Проверка

После деплоя откройте URL вашего сервиса и проверьте:
- ✅ Интерфейс загружается
- ✅ API endpoints работают (`/api/test`)
- ✅ Скрапер может запуститься

## 📝 Примечания

- Первый деплой может занять 10-15 минут (установка Chrome)
- Файлы в `uploads/` удаляются при перезапуске (эфемерная файловая система)
- Для длительных операций таймаут установлен на 5 минут

## 🆘 Решение проблем

**Проблема**: Ошибка при сборке  
**Решение**: Проверьте логи сборки, убедитесь что `build.sh` выполняется

**Проблема**: Chrome не установлен  
**Решение**: Проверьте логи, возможно нужно обновить `build.sh`

**Проблема**: Таймаут при работе скрапера  
**Решение**: Увеличьте `--timeout` в startCommand в `render.yaml`

---

Подробная документация: см. `README_RENDER.md`
