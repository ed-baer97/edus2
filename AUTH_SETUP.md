# 🔐 Настройка авторизации

## Автоматическая авторизация (для Render)

На сервере Render приложение работает в headless режиме, поэтому ручная авторизация невозможна. Используйте автоматическую авторизацию через переменные окружения.

### Настройка на Render

1. В настройках сервиса Render (Environment) добавьте:
   - `EDUS_LOGIN` = ваш логин
   - `EDUS_PASSWORD` = ваш пароль

2. Приложение автоматически:
   - Найдет поля ввода на странице авторизации
   - Введет логин и пароль
   - Выполнит вход

## Ручная авторизация (для локальной разработки)

Если переменные `EDUS_LOGIN` и `EDUS_PASSWORD` не установлены, приложение будет ожидать ручной авторизации:

1. Запустите приложение локально
2. Откроется браузер Chrome
3. Введите логин и пароль вручную
4. Нажмите кнопку входа
5. Приложение автоматически обнаружит успешную авторизацию

### Локальная настройка

Для локальной разработки можно:
- Не устанавливать переменные окружения (будет ручная авторизация)
- Или установить `HEADLESS=false` в `.env` файле

## Проверка работы

После настройки переменных окружения на Render:
1. Запустите скрапер через веб-интерфейс
2. В логах должно появиться: "Автоматическая авторизация..."
3. Через несколько секунд: "✓ Автоматическая авторизация успешна!"

## Решение проблем

**Проблема**: "Не удалось авторизоваться"  
**Решение**: 
- Проверьте, что `EDUS_LOGIN` и `EDUS_PASSWORD` установлены правильно
- Убедитесь, что учетные данные верны
- Проверьте логи для деталей ошибки

**Проблема**: Браузер не открывается локально  
**Решение**: 
- Убедитесь, что `HEADLESS=false` или не установлен
- Проверьте, что Chrome установлен на вашем компьютере
